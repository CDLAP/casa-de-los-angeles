// Legacy component — no longer in use. Replaced by SeasonPaquetes/SeasonFechas.
// Kept as a stub so TypeScript doesn't complain about stale imports.
// Safe to delete if confirmed unused.

export default function EventGrid() {
  return null
}
