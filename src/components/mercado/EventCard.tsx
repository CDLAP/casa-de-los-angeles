// Legacy component — no longer in use. Replaced by SeasonPaquetes for the
// Mercado de la Luna seasonal landing. Kept as a stub so TypeScript doesn't
// complain about stale imports. Safe to delete if confirmed unused.

export default function EventCard() {
  return null
}
